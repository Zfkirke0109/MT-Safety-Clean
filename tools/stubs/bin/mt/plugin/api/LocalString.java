package bin.mt.plugin.api;

/** Compile-only stub of MT Manager's localized string bundle. Not shipped. */
public interface LocalString {

    String get(String key);

    String get(String key, String defaultValue);
}
